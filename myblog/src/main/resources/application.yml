spring:
  thymeleaf:
    mode: HTML
  profiles:
    active: dev
  messages:
    basename: i18n/messages

comment.avatar: /images/avatar.png
message.avatar: /images/avatar.png

mybatis-plus:
  type-aliases-package: com.yyn1215.**.entity
  mapper-locations: classpath:mybatis/**/*.xml
